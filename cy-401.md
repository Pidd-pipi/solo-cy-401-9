<!-- 改编自：职业法律手工艺主题项目提示词/cy-401/cy-401.md；后端由 NestJS + TypeScript 改造为 Go 1.22 + Gin + GORM -->

请根据以下要求生成项目代码和 README。

## 项目主要需求

**项目概述**：自由职业者撮合平台 —— 连接需求方与自由职业者，支持需求发布、报价竞标、合同签订和项目交付全流程管理。

**项目类型**：全栈Web应用

**核心实体**（≥ 3 个，每个实体必须贯穿"数据库表 → 后端 model → 后端 service → 后端 handler/route → 前端 API 层 → 前端 store/状态 → 前端组件/页面"全链路）：

1. **需求（Requirement）**：标题、描述、预算范围（最低/最高）、截止日期、技能标签（多对多）、状态（待报价/进行中/待验收/已完成/已取消）、发布者ID、中标者ID。业务动作：创建需求、编辑需求、需求列表筛选、状态流转。
   - 贯穿全栈：DB表 → `backend/internal/model/requirement.go` → `backend/internal/service/requirement_service.go` → `backend/internal/handler/requirement_handler.go` → `backend/internal/router/requirements.go` → `frontend/src/api/requirement.ts` → `frontend/src/stores/requirement.ts` → `frontend/src/components/common/RequirementCard.vue`
2. **报价（Bid）**：报价金额、工期（天）、提案内容、附件列表、状态（待审/已采纳/已拒绝/已撤回）、需求ID、报价者ID。业务动作：提交报价、查看报价、比较报价、接受/拒绝报价、撤回报价。
   - 贯穿全栈：DB表 → `backend/internal/model/bid.go` → `backend/internal/service/bid_service.go` → `backend/internal/handler/bid_handler.go` → `backend/internal/router/bids.go` → `frontend/src/api/bid.ts` → `frontend/src/stores/bid.ts` → `frontend/src/components/common/BidCard.vue`
3. **合同（Contract）**：合同编号、总金额、付款方式（分阶段/一次性）、阶段列表（JSON）、状态（待签署/执行中/待验收/已完成/已终止）、需求ID、甲方ID、乙方ID。业务动作：生成合同、签署确认、查看合同列表、完成确认。
   - 贯穿全栈：DB表 → `backend/internal/model/contract.go` → `backend/internal/service/contract_service.go` → `backend/internal/handler/contract_handler.go` → `backend/internal/router/contracts.go` → `frontend/src/api/contract.ts` → `frontend/src/stores/contract.ts` → `frontend/src/components/common/ContractCard.vue`
4. **用户（User）**：用户名、邮箱、角色（需求方/自由职业者/双角色）、头像、技能标签、评分、简介、联系方式。贯穿全栈认证与个人中心。
   - 贯穿全栈：DB表 → `backend/internal/model/user.go` → `backend/internal/service/user_service.go` / `auth_service.go` → `backend/internal/handler/user_handler.go` / `auth_handler.go` → `backend/internal/router/users.go` / `auth.go` → `frontend/src/api/user.ts` / `auth.ts` → `frontend/src/stores/user.ts` → `frontend/src/pages/Profile.vue`

**核心页面**（≥ 4 个）：
1. **需求大厅** `/requirements`：瀑布流/列表展示所有需求，支持按预算范围、技能、状态多维度筛选。消费需求实体、用户实体。使用共享组件 `<RequirementCard>` 和 `<FilterBar>`。
2. **需求详情** `/requirements/:id`：展示需求完整信息、已提交报价列表（需求方视角）、报价提交表单（自由职业者视角）。消费需求、报价、用户三个实体。使用 `<BidCard>` 组件。
3. **我的工作台** `/dashboard`：分角色展示——需求方看已发布需求和收到的报价，自由职业者看已报价项目和进行中合同。消费需求、报价、合同三个实体。使用 `<StatusBadge>` 和 `<ContractCard>`。
4. **合同详情** `/contracts/:id`：展示合同条款、阶段进度、双方信息。消费合同、用户实体。使用 `<StatusBadge>` 和 `<ProgressSteps>`。
5. **个人资料** `/profile/:id`：展示/编辑个人信息、技能标签、历史项目。消费用户、需求、合同实体。使用 `<UserAvatar>` 和 `<SkillTag>`。

**横切关注点**（≥ 2 个，写明每个触达的文件层）：
1. **认证授权（JWT）**：触达层——DB: users表 + 后端: `backend/internal/middleware/auth.go` / `backend/internal/util/jwt.go` + 前端: router守卫 + 前端: useAuth hook + 前端: 请求拦截器自动带token。
2. **操作日志**：触达层——DB: operation_logs表 + 后端: `backend/internal/middleware/audit_logger.go`（记录关键操作） + 前端: 无直接触达，但在管理页面可查看日志列表。

**共享枚举/常量**（≥ 2 组，明确前后端定义位置）：
1. **需求状态（RequirementStatus）**：draft / open / bidding / in_progress / pending_review / completed / cancelled
   - 后端定义位置：`backend/internal/constants/requirement_status.go`
   - 前端定义位置：`frontend/src/types/enums.ts`
2. **报价状态（BidStatus）**：pending / accepted / rejected / withdrawn
   - 后端定义位置：`backend/internal/constants/bid_status.go`
   - 前端定义位置：`frontend/src/types/enums.ts`
> README 中必须列出上述枚举在前后端的所有出现位置。

**共享前端组件清单**（≥ 3 个，放在 `components/common/`）：
1. `<RequirementCard>`：被需求大厅、工作台、个人资料页消费
2. `<StatusBadge>`：被需求详情、工作台、合同详情页消费
3. `<BidCard>`：被需求详情、工作台页消费
4. `<UserAvatar>`：被需求详情、合同详情、个人资料页消费

**共享 hooks/utils 清单**（≥ 2 个）：
1. `useAuth()`：封装登录/登出/token刷新/用户信息获取，被所有需认证页面消费
2. `usePagination()`：封装分页逻辑（页码/每页数量/总数/翻页），被需求列表、报价列表、合同列表页消费
3. `formatCurrency()`：格式化金额显示，被报价卡片、合同详情等页面消费

**后端中间件清单**（≥ 2 个）：
1. `backend/internal/middleware/auth.go`：JWT验证，注入当前用户，被所有需认证路由使用
2. `backend/internal/middleware/error_handler.go`：全局错误捕获，统一返回 { code, message, details } 格式
3. `backend/internal/middleware/request_logger.go`：记录请求方法、路径、状态码、响应时间
4. `backend/internal/middleware/audit_logger.go`：记录关键操作日志

**技术要求**：
- 英文短名（用于 Docker Compose）：`cyfreelance`
- 前端端口：`28030`，后端端口：`29068`（本地）/ `8080`（Docker内部），数据库端口：`38101`
- 前端框架：Vue 3 + TypeScript
- UI 库：Element Plus
- 构建工具：Vite
- 后端框架：Go 1.22 + Gin（github.com/gin-gonic/gin）
- ORM：GORM（gorm.io/gorm + gorm.io/driver/mysql）
- 数据库：MySQL 8.0
- 认证方式：JWT（github.com/golang-jwt/jwt/v5，bcrypt 加密密码 golang.org/x/crypto/bcrypt）
- 其他依赖：github.com/go-playground/validator/v10（DTO校验）、github.com/gin-contrib/cors（跨域）、文件上传（附件列表，本地磁盘或对象存储路径存储）
- API 文档：github.com/swaggo/gin-swagger（挂载于 `/docs`）

**文件结构强制清单**（严禁合并职责到单一文件，本项目用于验证跨文件协同改动能力）：

前端必须包含：
```
frontend/src/
├── api/         # 按实体分文件：requirement.ts, bid.ts, contract.ts, user.ts, auth.ts
├── stores/      # 按实体分文件：requirement.ts, bid.ts, contract.ts, user.ts
├── types/       # 共享类型定义 + enums.ts（枚举常量）
├── components/common/  # ≥ 4 个共享组件
├── hooks/       # ≥ 2 个自定义 hook
├── pages/       # ≥ 5 个页面
├── router/
└── utils/
```

后端必须包含（Go 标准布局）：
```
backend/
├── go.mod
├── go.sum
├── Dockerfile
├── cmd/
│   └── server/
│       └── main.go
├── database/
│   └── init.sql          # 建表 SQL + 种子数据
└── internal/
    ├── config/           # config.go（MySQL、JWT、Server）
    ├── model/            # user.go, requirement.go, bid.go, contract.go, operation_log.go
    ├── repository/       # user_repository.go, requirement_repository.go, bid_repository.go, contract_repository.go
    ├── service/          # auth_service.go, user_service.go, requirement_service.go, bid_service.go, contract_service.go
    ├── handler/          # auth_handler.go, user_handler.go, requirement_handler.go, bid_handler.go, contract_handler.go
    ├── router/           # router.go, auth.go, users.go, requirements.go, bids.go, contracts.go
    ├── middleware/       # auth.go, audit_logger.go, error_handler.go, request_logger.go
    ├── dto/              # auth_dto.go, user_dto.go, requirement_dto.go, bid_dto.go, contract_dto.go
    ├── constants/        # requirement_status.go, bid_status.go, contract_status.go, roles.go, error_codes.go
    ├── util/             # jwt.go, password.go, upload.go（附件上传）, response.go
    └── logger/           # logger.go（log/slog）
```

**约束的目的**：
> 本项目用于验证跨文件协同改动能力，禁止将多个职责合并进单一文件。即使是简单功能，也必须按照上述分层结构拆分，确保后续每一个小改动都至少触达 3-5 个文件。

## 后端目录结构（Go 标准布局，强制执行）

后端代码放在 `backend/`，采用 `cmd/` + `internal/` 分层：

- `backend/cmd/server/main.go`：程序入口，加载配置、装配依赖、启动 Gin 服务
- `backend/internal/config/`：环境变量配置解析
- `backend/internal/model/`：GORM 数据模型（按实体独立文件）
- `backend/internal/repository/`：数据访问层（按实体独立文件）
- `backend/internal/service/`：业务逻辑层（按实体独立文件）
- `backend/internal/handler/`：HTTP 处理层（等价于 controller，按实体独立文件）
- `backend/internal/router/`：路由注册（按实体独立文件）
- `backend/internal/middleware/`：JWT 鉴权、审计日志、全局错误、请求日志中间件
- `backend/internal/dto/`：请求/响应结构体（validator tag 声明校验规则）
- `backend/internal/constants/`：枚举、错误码、角色常量
- `backend/internal/util/`：jwt、密码哈希、附件上传、统一响应工具
- `backend/internal/logger/`：log/slog 结构化日志
- `backend/database/`：init.sql 建表 + 种子数据

后端本地构建与运行命令：
- 依赖安装：`go mod tidy`
- 编译检查：`go build ./...`
- 本地运行：`go run ./cmd/server`

## Go 企业级工程规范（必须遵循）

后端 Go 代码除满足上述功能与分层要求外，还必须遵循当前 Go 语言通用的企业级工程规范：

### 1. 项目目录规范
- 根目录 `go.mod` 声明 Go 版本 `go 1.22`，模块名使用公司/项目规范（如 `github.com/<org>/<project>`）。
- 采用 `cmd/` + `internal/` + `pkg/` 布局：`cmd/server/main.go` 只负责加载配置、装配依赖、启动服务；私有业务代码全部放在 `internal/`；仅无业务依赖的可复用库放 `pkg/`。
- 数据库迁移脚本放 `migrations/`，接口定义/OpenAPI 文档放 `api/`，Docker/K8s 部署文件放 `deploy/`。
- 包名使用小写单词、不加下划线；文件使用 `snake_case.go`；导出标识符用 `CamelCase`，未导出的用 `camelCase`；禁止使用拼音命名。

### 2. 分层与依赖方向
- 依赖方向严格单向：`handler → service → repository → model`，`model` 不依赖任何上层，禁止反向引用与包循环依赖。
- 每个核心实体按 `model_<entity>.go`、`repository_<entity>.go`、`service_<entity>.go`、`handler_<entity>.go` 分文件，职责单一。
- 依赖通过构造函数注入：`func NewXService(repo Repo, logger *slog.Logger) *XService`；禁止在业务代码中全局 `init` 连接数据库或散落 `sql.Open`。

### 3. 错误处理
- 使用 `fmt.Errorf("list users: %w", err)` 包裹并透传错误，保留错误链。
- 仓储层定义哨兵错误（如 `var ErrNotFound = errors.New("not found")`），上层用 `errors.Is` 判断。
- 禁止在 service 层打印日志后吞掉错误；错误统一由 `middleware` 或 `handler` 转换为标准 JSON 响应。
- 所有 error 必须被处理，关键错误禁止 `_ =` 忽略。

### 4. 配置与日志
- 配置通过环境变量注入，在 `internal/config/config.go` 集中解析，推荐使用 `github.com/caarlos0/env/v11` 或 `spf13/viper`。
- 日志使用 `log/slog` 或 `go.uber.org/zap` 结构化输出；请求日志统一包含 `request_id`、`method`、`path`、`status`、`latency_ms`。

### 5. 参数校验与响应
- 入参校验使用 `github.com/go-playground/validator/v10`，在 DTO 结构体 tag 中声明规则。
- 路由统一 `/api/v1` 前缀；健康检查为 `/healthz`；分页参数统一为 `page`、`page_size`。
- 响应统一为 `{ "code": 0, "message": "ok", "data": ... }` 包裹，错误码集中维护。

### 6. 测试与文档
- 单元测试使用标准库 `testing` + 表驱动（table-driven），文件命名 `*_test.go`；service 与 repository 必须有对应测试。
- 对外接口提供 OpenAPI/Swagger（如 `github.com/swaggo/gin-swagger`）或在 README 中列出完整 API 清单。

## 代码存放规则

这是前后端分离项目。

前端代码放在 `frontend/`：
页面，组件，路由,状态管理,API 请求,类型定义,样式和资源等放在对应的子文件夹

后端代码放在 `backend/`：
数据库脚本放在 `database/`。

## 部署要求

项目必须支持 Docker Compose 一键部署，且在 **任意目录名（包括中文目录名）** 下都能正常启动。

需要提供以下文件：
- 根目录 `docker-compose.yml`：编排前端、后端、数据库及所有依赖服务
- 根目录 `.env`：包含 `COMPOSE_PROJECT_NAME=cyfreelance`，确保 Compose 项目名不依赖目录名
- `frontend/Dockerfile`：前端项目构建与运行镜像（多阶段构建，先构建再用 Nginx 托管）
- `backend/Dockerfile`：Go 多阶段构建（见下方骨架）
- `frontend/nginx.conf`：Nginx 配置，处理前端路由和 API 反向代理
- `.env.example`：所有环境变量的示例文件，附注释说明

backend/Dockerfile 使用以下 Go 多阶段构建骨架：

```dockerfile
FROM golang:1.22-alpine AS builder
WORKDIR /app
COPY go.mod go.sum ./
RUN go mod download
COPY . .
RUN CGO_ENABLED=0 GOOS=linux go build -o /app/server ./cmd/server
FROM alpine:3.20
RUN apk add --no-cache ca-certificates tzdata
WORKDIR /app
COPY --from=builder /app/server .
EXPOSE 8080
CMD ["/app/server"]
```

docker-compose.yml 要求：
- **不要写 `version:` 字段**（Compose V2 已废弃）
- 顶层声明 `name: cyfreelance` 作为兜底
- 所有服务使用 `container_name` 命名容器，容器名带 `${COMPOSE_PROJECT_NAME:-cyfreelance}` 前缀
- 数据库数据使用命名卷持久化
- 前端通过 Nginx 反向代理后端 API（前端代码里调用 `/api`，由 Nginx 转发到 `http://backend:8080/`，不要硬编码 `localhost`）
- 数据库服务配置 `healthcheck`，后端 `depends_on: { db: { condition: service_healthy } }`
- 后端也配置 `healthcheck`，前端 `depends_on` 后端
- 暴露端口：前端 `${FRONTEND_PORT:-28030}:80`，后端 `${BACKEND_PORT:-29068}:8080`，数据库 `${DB_PORT:-38101}:3306`
- 所有可变配置通过环境变量注入

`.env` / `.env.example` 必须包含的键：
- `COMPOSE_PROJECT_NAME=cyfreelance`
- `DB_NAME` / `DB_USER` / `DB_PASSWORD` / `DB_ROOT_PASSWORD`
- `JWT_SECRET`
- `FRONTEND_PORT` / `BACKEND_PORT` / `DB_PORT`

Nginx 反向代理要求（`frontend/nginx.conf`）：
- `location /api/` 反向代理到 `http://backend:8080/`
- 配置 `try_files $uri $uri/ /index.html;` 支持前端 SPA 路由

自检清单（生成代码后必须确认）：
1. 在中文目录名下执行 `docker compose config --quiet` 不报错
2. 启动后 `docker compose ps` 显示全部容器 healthy
3. 浏览器访问前端端口能看到页面，`/api/...` 请求返回正常
4. README 中 Docker 启动命令是 `docker compose up -d`
5. 后端在 `backend/` 下执行 `go mod tidy` 与 `go build ./...` 不报错
6. 核心实体 ≥ 3 个，每个实体明确贯穿全栈；共享枚举在 README 中列出所有出现位置

## README 写法

根目录必须有 `README.md`，用中文说明项目。

README 包含：
- 项目名称和一句话介绍
- 项目主要功能
- 快速启动方式（Docker Compose 一键部署命令）
- 本地开发方式（前端启动命令、后端启动命令：`cd backend && go mod tidy && go run ./cmd/server`）
- 访问地址
- 技术栈（表格展示，后端一栏写 `Go 1.22 + Gin + GORM`）
- 项目目录结构（代码块展示）
- 环境变量说明
- Docker 部署说明（端口映射、数据卷、常见问题）
- 枚举出现位置清单（RequirementStatus 和 BidStatus 在前后端的所有出现位置）
- License

README 要求：
- Docker 部署命令放在最前面
- 命令可直接复制运行
- 不要保留脚手架默认 README 内容

## docker-compose.yml 参考骨架（务必遵循）

```yaml
name: cyfreelance

services:
  db:
    image: mysql:8.0
    container_name: ${COMPOSE_PROJECT_NAME:-cyfreelance}-db
    restart: unless-stopped
    ports:
      - "${DB_PORT:-38101}:3306"
    environment:
      MYSQL_ROOT_PASSWORD: ${DB_ROOT_PASSWORD}
      MYSQL_DATABASE: ${DB_NAME}
      MYSQL_USER: ${DB_USER}
      MYSQL_PASSWORD: ${DB_PASSWORD}
    volumes:
      - db_data:/var/lib/mysql
    healthcheck:
      test: ["CMD", "mysqladmin", "ping", "-h", "localhost", "-u", "root", "-p${DB_ROOT_PASSWORD}"]
      interval: 5s
      timeout: 5s
      retries: 20

  backend:
    build: ./backend
    container_name: ${COMPOSE_PROJECT_NAME:-cyfreelance}-backend
    restart: unless-stopped
    ports:
      - "${BACKEND_PORT:-29068}:8080"
    environment:
      APP_ENV: production
      SERVER_PORT: 8080
      DB_HOST: db
      DB_PORT: 3306
      DB_NAME: ${DB_NAME}
      DB_USER: ${DB_USER}
      DB_PASSWORD: ${DB_PASSWORD}
      JWT_SECRET: ${JWT_SECRET}
    depends_on:
      db:
        condition: service_healthy
    healthcheck:
      test: ["CMD-SHELL", "wget -q -O /dev/null http://localhost:8080/healthz || exit 1"]
      interval: 10s
      timeout: 5s
      retries: 20

  frontend:
    build: ./frontend
    container_name: ${COMPOSE_PROJECT_NAME:-cyfreelance}-frontend
    restart: unless-stopped
    ports:
      - "${FRONTEND_PORT:-28030}:80"
    depends_on:
      backend:
        condition: service_healthy

volumes:
  db_data:
```

`.env.example` 参考：

```dotenv
COMPOSE_PROJECT_NAME=cyfreelance
DB_NAME=freelance
DB_USER=freelance
DB_PASSWORD=freelance_pwd
DB_ROOT_PASSWORD=root_pwd
JWT_SECRET=change_me_to_a_long_random_string
FRONTEND_PORT=28030
BACKEND_PORT=29068
DB_PORT=38101
```

README 中务必告知用户首次启动前执行 `cp .env.example .env`。

## Git 要求

代码编写完成后初始化 Git + 创建 `.gitignore`，`.gitignore` 内容按项目技术栈适配（至少包含）：

```
node_modules/
dist/
.env
.DS_Store
*.log
.idea/
.vscode/
backend/server
backend/bin/
*.test
*.out
coverage.out
frontend/coverage/
```
